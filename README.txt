Created by AlexVide0 / Alex Thompson-Hamilton

Find my everything on alexvide0.com

Within the config file:
	- ip_address is the companion ip address (if on the same machine, 127.0.0.1 or localhost will do)
	- port_number is the companion port number (usually 8000 by default)

	- add keybinds by copy/pasting one of the existing keybinds (text-based UI incoming for this)
	- combination keys (eg control + 1) not supported